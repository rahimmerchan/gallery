export default class ImageObject {
  constructor(url, title, year, desc, author) {
    this.url = url;
    this.title = title;
    this.year = year;
    this.desc = desc;
    this.author = author;
  }
}
